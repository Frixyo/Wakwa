1. Install dependencies

   ```bash
      npm install
   ```

2. Start the app

   ```bash
      npm run start
   ```

## Reinstaler la BD

   go into assets/db
   delete mydatabase.db
   ```bash
      sqlite3 mydatabase.db < init.sql
   ```
